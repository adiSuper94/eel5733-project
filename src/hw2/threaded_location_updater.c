#include "../hw1/filter.h"
#include <pthread.h>
#include <stdio.h>

int main() {
  // pthread_t emailFilterThread;
  // printf("running as a thread\n");
  // pthread_create(&emailFilterThread, NULL, (void *)*runEmailFilter, NULL);
  // printf("waiting for thread to complete\n");
  // pthread_exit(NULL);
}
